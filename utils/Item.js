export default class Item {
  constructor(id, title, amount, date, imageURI, category) {
    (this.id = id),
      (this.title = title),
      (this.amount = amount),
      (this.date = date),
      (this.imageURI = imageURI),
      (this.category = category);
  }
}

// has expire date
